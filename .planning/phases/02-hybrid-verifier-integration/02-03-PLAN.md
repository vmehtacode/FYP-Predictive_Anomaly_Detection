---
phase: 02-hybrid-verifier-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - tests/test_hybrid_verifier.py
autonomous: true
requirements:
  - GNN-03
  - ENS-01
  - ENS-02

must_haves:
  truths:
    - "All physics constraint scoring edge cases tested (safe zone, warning zone, full violation, boundary values)"
    - "Ensemble score combination tested for both normal nodes and early-exited nodes"
    - "HybridVerifierAgent is a verified drop-in replacement for VerifierAgent (same call patterns work)"
    - "Confidence-based reward tested for correct/incorrect predictions with asymmetric penalties"
    - "All tests pass with pytest"
  artifacts:
    - path: "tests/test_hybrid_verifier.py"
      provides: "Comprehensive test suite for hybrid verifier components"
      contains: "test_hybrid_verifier_evaluate_matches_interface"
  key_links:
    - from: "tests/test_hybrid_verifier.py"
      to: "src/fyp/selfplay/hybrid_verifier.py"
      via: "Tests import and exercise all public classes and functions"
      pattern: "from fyp.selfplay.hybrid_verifier import"
    - from: "tests/test_hybrid_verifier.py"
      to: "src/fyp/selfplay/trainer.py"
      via: "Integration test verifies HybridVerifierAgent works with trainer call patterns"
      pattern: "verifier\\.evaluate.*forecast.*scenario.*return_details"
---

<objective>
Create comprehensive tests for all hybrid verifier components: physics constraint scoring, cascade logic, ensemble combination, reward computation, and drop-in replacement compatibility.

Purpose: Verify that the hybrid verifier produces correct scores, handles edge cases, and integrates as a drop-in replacement for the existing VerifierAgent without breaking the SelfPlayTrainer.

Output: `tests/test_hybrid_verifier.py` with unit tests for each component and integration tests for the full pipeline.
</objective>

<execution_context>
@/Users/vatsalmehta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vatsalmehta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hybrid-verifier-integration/02-RESEARCH.md
@.planning/phases/02-hybrid-verifier-integration/02-01-SUMMARY.md
@.planning/phases/02-hybrid-verifier-integration/02-02-SUMMARY.md
@src/fyp/selfplay/hybrid_verifier.py
@src/fyp/selfplay/hybrid_verifier_config.py
@src/fyp/selfplay/verifier.py
@src/fyp/selfplay/trainer.py
@tests/test_gnn/test_gat_verifier.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for config, physics layer, cascade layer, and ensemble scoring</name>
  <files>tests/test_hybrid_verifier.py</files>
  <action>
Create `tests/test_hybrid_verifier.py` with pytest-style tests following the pattern from `tests/test_gnn/`. Use pytest fixtures for reusable test data.

**Fixtures:**
- `default_config()` -> HybridVerifierConfig with defaults
- `physics_layer(default_config)` -> PhysicsConstraintLayer
- `cascade_layer(default_config)` -> CascadeLogicLayer
- `simple_edge_index()` -> 5-node line graph edge_index (0-1-2-3-4) as torch.LongTensor
- `hybrid_verifier(default_config)` -> HybridVerifierAgent in physics-only mode (no GNN)

**Config tests (`TestHybridVerifierConfig`):**
- `test_default_config_valid` — HybridVerifierConfig() creates valid config with expected defaults
- `test_config_from_yaml` — load_hybrid_verifier_config('configs/hybrid_verifier.yaml') loads correctly
- `test_config_voltage_defaults` — nominal_v=230, limits match BS EN 50160
- `test_config_ensemble_weights` — physics=0.4, gnn=0.4, cascade=0.2
- `test_config_invalid_threshold_rejected` — Pydantic rejects early_exit_threshold > 1 or < 0

**Tolerance band scoring tests (`TestToleranceBandScore`):**
- `test_safe_zone_returns_zero` — value in [lower_safe, upper_safe] -> 0.0
- `test_lower_limit_returns_one` — value below lower_limit -> 1.0
- `test_upper_limit_returns_one` — value above upper_limit -> 1.0
- `test_lower_warning_zone_interpolation` — value between lower_limit and lower_safe -> linear 0-1
- `test_upper_warning_zone_interpolation` — value between upper_safe and upper_limit -> linear 0-1
- `test_boundary_values` — exact boundary values (lower_safe, upper_safe, lower_limit, upper_limit)
- `test_midpoint_warning_zone` — midpoint of warning zone should be ~0.5

**Physics constraint layer tests (`TestPhysicsConstraintLayer`):**
- `test_normal_forecast_low_scores` — all values at 230V -> scores near 0
- `test_voltage_violation_high_scores` — values at 260V -> scores near/at 1.0
- `test_capacity_violation` — values exceeding absolute_max_kw -> high scores
- `test_ramp_rate_violation` — large consecutive differences -> high ramp scores
- `test_output_shape_matches_input` — scores array length == forecast length
- `test_scores_in_range` — all scores in [0, 1]
- `test_details_contains_all_constraints` — details dict has voltage_scores, capacity_scores, ramp_scores, combined_scores

**Cascade logic tests (`TestCascadeLogicLayer`):**
- `test_isolated_anomaly_low_cascade` — single anomalous node with normal neighbors -> low cascade
- `test_propagating_anomaly_high_cascade` — cluster of anomalous nodes -> high cascade for connected nodes
- `test_no_anomaly_zero_cascade` — all normal nodes -> all cascade scores 0
- `test_empty_graph_no_crash` — handle graph with no edges gracefully
- `test_cascade_scores_in_range` — all scores in [0, 1]

**Ensemble combination tests (`TestEnsembleCombination`):**
- `test_normal_nodes_weighted_average` — non-exited nodes use configured weights
- `test_early_exit_nodes_physics_only` — exited nodes get physics score, gnn and cascade ignored
- `test_mixed_early_exit` — some nodes exited, some not; verify correct per-node handling
- `test_breakdown_contains_all_fields` — combined_scores, physics_scores, gnn_scores, cascade_scores, early_exit_mask, early_exit_count, weights
  </action>
  <verify>
Run: `python -m pytest tests/test_hybrid_verifier.py -v --tb=short -k "not Integration" 2>&1 | tail -30` — all unit tests should pass.
  </verify>
  <done>
All unit tests pass for config validation, tolerance band scoring (all zones and boundaries), physics constraint layer (voltage/capacity/ramp), cascade logic (isolated vs propagating anomalies), and ensemble combination (normal vs early-exit nodes). Tests cover edge cases including empty graphs and boundary values.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for HybridVerifierAgent drop-in replacement</name>
  <files>tests/test_hybrid_verifier.py</files>
  <action>
Add integration test class to `tests/test_hybrid_verifier.py`:

**Reward computation tests (`TestRewardComputation`):**
- `test_correct_normal_detection_positive_reward` — normal forecast, no scenario -> positive reward
- `test_correct_anomaly_detection_positive_reward` — anomalous forecast, scenario present -> positive reward
- `test_false_negative_high_penalty` — normal-looking forecast when scenario present -> negative reward, higher magnitude due to asymmetric penalty
- `test_false_positive_penalty` — anomalous-looking forecast when no scenario -> negative reward
- `test_reward_in_range` — reward always in [-1, +1] for various inputs
- `test_asymmetric_penalty_ratio` — false negative penalty is config.false_negative_penalty_ratio times false positive penalty for same confidence level

**Drop-in replacement integration tests (`TestHybridVerifierIntegration`):**
- `test_evaluate_signature_matches_verifier_agent` — use `inspect.signature()` to verify parameter names and defaults match VerifierAgent.evaluate()
- `test_evaluate_returns_float` — evaluate(forecast) returns float
- `test_evaluate_returns_tuple_with_details` — evaluate(forecast, return_details=True) returns (float, dict)
- `test_evaluate_with_scenario_parameter` — evaluate(forecast, scenario=None) works (even if scenario is None, the parameter must be accepted)
- `test_evaluate_with_timestamps_parameter` — evaluate(forecast, timestamps=None) works
- `test_trainer_call_pattern_training` — simulate trainer line 160-161: `verifier.evaluate(forecast=median_forecast, scenario=scenario, return_details=True)` returns (float, dict)
- `test_trainer_call_pattern_validation` — simulate trainer line 378: `verifier.evaluate(median_forecast, scenario)` returns float
- `test_physics_only_mode_works` — HybridVerifierAgent without GNN model still produces valid rewards

**Edge case tests:**
- `test_single_element_forecast` — forecast with 1 element
- `test_large_forecast` — forecast with 1000 elements
- `test_all_zeros_forecast` — all zeros (edge case for capacity/ramp)
- `test_negative_values_forecast` — negative forecast values (edge case for voltage)

For the `test_evaluate_signature_matches_verifier_agent` test, compare parameter names (forecast, scenario, timestamps, return_details) and that defaults are compatible (scenario=None, timestamps=None, return_details=False).

Import `inspect` for signature comparison. Use a mock ScenarioProposal or None for scenario parameter tests.
  </action>
  <verify>
Run: `python -m pytest tests/test_hybrid_verifier.py -v --tb=short 2>&1 | tail -40` — ALL tests (unit + integration) should pass.
  </verify>
  <done>
All tests pass including integration tests verifying drop-in replacement compatibility. HybridVerifierAgent matches VerifierAgent.evaluate() signature, handles both trainer call patterns (training with return_details=True, validation without), produces valid [-1,+1] rewards, and handles edge cases (single element, large arrays, zeros, negatives). Asymmetric false negative penalty verified.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_hybrid_verifier.py -v --tb=short` — all tests pass
2. `python -m pytest tests/test_hybrid_verifier.py -v -k "signature"` — signature match test specifically passes
3. `python -m pytest tests/test_hybrid_verifier.py -v -k "trainer_call"` — both trainer call pattern tests pass
4. `python -m pytest tests/test_hybrid_verifier.py --tb=short -q` — summary shows 0 failures
</verification>

<success_criteria>
- All unit tests pass for tolerance band scoring, physics layer, cascade layer, and ensemble combination
- All integration tests pass for drop-in replacement compatibility with SelfPlayTrainer
- Signature match verified via inspect.signature comparison
- Both trainer call patterns tested (training with return_details=True, validation without)
- Reward range [-1,+1] verified for normal, anomalous, and edge case inputs
- Asymmetric false negative penalty verified to be config.false_negative_penalty_ratio times FP penalty
- Edge cases (single element, large arrays, zeros, negatives) handled without crashes
</success_criteria>

<output>
After completion, create `.planning/phases/02-hybrid-verifier-integration/02-03-SUMMARY.md`
</output>
